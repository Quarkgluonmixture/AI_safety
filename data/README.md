# Data Directory

This directory is intentionally empty except for placeholders.

- `data/pilot/` is where the researcher supplies `pilot_v1.jsonl`.
- `data/targets/` is where optional target/lens/voice/frame metadata can be placed.

The pipeline must not invent stimuli. User-authored pilot rows and metadata remain the source of truth.

